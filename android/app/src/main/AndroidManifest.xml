<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 13+ 推送通知权限：与源工程 car/app/src/main/AndroidManifest.xml 保持一致 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <application
        android:label="中导物联"
        android:usesCleartextTraffic="true"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Flutter 3.29+ 默认开启深链接：App 进程被杀后点击厂商通道离线推送时，
                 intent 携带的 URI（含 n_extra）会被当作 Flutter 路由解析导致白屏。
                 本项目未使用 Flutter 官方深链接，显式关闭。 -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false" />
        </activity>
        <!-- 极光推送 AppKey / 渠道，取值来自 build.gradle.kts 的 manifestPlaceholders -->
        <meta-data
            android:name="JPUSH_APPKEY"
            android:value="${JPUSH_APPKEY}" />
        <meta-data
            android:name="JPUSH_CHANNEL"
            android:value="${JPUSH_CHANNEL}" />
        <!-- 厂商通道 meta-data（HUAWEI_APPID / HONOR_APPID / OPPO_* 等）由极光厂商 SDK AAR 自带，
             其值取自 app/build.gradle.kts 的 manifestPlaceholders，此处不重复声明，否则会
             触发 manifest 合并冲突（duplicate meta-data）。
             例外：极光 6.2.1 的小米 AAR 值模板末尾多了一个反斜杠（"${XIAOMI_APPID}\"），
             替换后 AppID 会变成 "2882303761520585420\"，导致小米通道注册失败，
             因此这里用 tools:replace 覆盖为不含反斜杠的正确值。 -->
        <meta-data
            android:name="XIAOMI_APPID"
            android:value="${XIAOMI_APPID}"
            tools:replace="android:value" />
        <meta-data
            android:name="XIAOMI_APPKEY"
            android:value="${XIAOMI_APPKEY}"
            tools:replace="android:value" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
